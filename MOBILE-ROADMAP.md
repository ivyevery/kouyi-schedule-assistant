# 口译日程手机端升级路线图

## 目标

先把现有电脑端工具变成手机浏览器可用的轻量日程工具，再逐步加入可靠的跨设备同步。第一阶段不急于决定 App 还是小程序，先把数据模型和手机端核心体验做稳。

## 手机端导航与信息顺序

1. 主页：今天的日期与状态、可左右轮转的日期条、点选日期的当天日程、最近 5 场会议。
2. 月历：圆点式迷你月历、全部日程和四种状态筛选。
3. 加号：从底部导航直接打开新增日程表单。
4. 设置：数据管理、同步、微信导出和外观设置。

## 已完成：手机 HTML 首版

- 独立文件 `mobile-schedule.html`，不改动现有电脑端页面。
- 应用“小日常 UI 设计语言 V1.0”，统一桃粉奶油底色、深橙主题、宋体标题与三色状态体系。
- 适配 320px 以上手机屏幕和安全区域。
- 主页自然周日期条和最近 5 个 Upcoming 日程。
- 日期条可按自然周前后切换，点选后停留主页展开当天日程。
- 五行或六行自适应圆形月历、月份切换、回到今天、日期选择。
- 底部四个等宽图标入口，图标资源随项目本地保存并自动跟随主题颜色。
- 全部、预定、确认、完成筛选。
- 新建日程使用独立整页流程，包含文字快速录入、浏览器语音输入、信息识别与结构化表单确认。
- 新增、编辑和删除日程。
- 与电脑端相同的数据结构。
- 本机自动保存和最近 30 份滚动备份。
- JSON 导入和导出。
- 系统暗色模式、减少动效和键盘焦点支持。

## 下一步：让手机真正访问

1. 把两个 HTML 页面放入一个本地静态站点目录。
2. 在同一 Wi-Fi 下用电脑启动本地服务，手机通过局域网地址访问。
3. 在 iPhone 和 Android 各测试一次：日历点击、表单键盘、滚动、导入导出。
4. 将手机页面加入主入口，并为电脑端保留原入口。

这一阶段仍然是每台设备各自保存数据。可以用 JSON 手动传递，但不应把它称为自动同步。

## 核心升级：可靠的跨设备同步

推荐采用“网页应用 + 托管数据库”的结构。手机和电脑都访问同一个网址，登录后读取同一份数据。

### 数据层

- 选用托管数据库和账号系统，例如 Supabase。
- 每条日程增加 `user_id`、`created_at`、`updated_at` 和 `deleted_at`。
- 数据库开启行级权限，每个账号只能访问自己的数据。
- 服务端保留定期备份，客户端继续保留离线缓存。

### 同步规则

- 每次修改先写入本机缓存，再上传云端。
- 网络恢复后自动重试未上传的修改。
- 用 `updated_at` 判断新旧，检测到两台设备同时修改同一条记录时提示选择。
- 删除采用可恢复的软删除，避免一次误操作永久丢失。
- 首次上线前提供旧 JSON 一键迁移。

### 数据稳定性验收

- 手机断网时仍可查看和新增日程。
- 恢复网络后自动同步，且不会生成重复记录。
- 两台设备依次修改时结果一致。
- 同时修改同一条记录时不会静默覆盖。
- 误删记录可以恢复。
- 云端备份可以实际还原，而不只是“已经开启备份”。

## 上线形态的选择顺序

### 推荐先做 PWA 网页应用

- 复用当前 HTML 和数据模型。
- 手机浏览器可添加到主屏幕，外观接近 App。
- 一套代码同时服务电脑和手机。
- 上线和迭代速度最快。

### 再决定是否做小程序

当微信内分享、微信登录或微信提醒成为高频需求时，再把稳定的数据接口接到小程序。小程序应复用同一后端，不另建一套数据。

### 最后考虑原生 App

只有在需要系统级推送、深度日历集成、离线能力或应用商店分发时，原生 App 的额外成本才更值得。

## 建议的实施批次

### 可用性批次

- 在真实手机上测试首版。
- 调整月历高度、文字大小和表单字段顺序。
- 确认手机端最常用的三个动作。

### 云同步批次

- 建数据库、登录和权限。
- 完成旧数据迁移。
- 加入离线队列、冲突处理和同步状态。
- 通过断网、双设备和恢复测试。

### PWA 上线批次

- 加入应用图标和主屏幕安装。
- 加入离线壳和版本更新提示。
- 部署到 HTTPS 域名。
- 做一次完整的数据恢复演练。

### 功能扩展批次

- 快速语音录入。
- 会前提醒和日历订阅。
- 会后纪要与资料整理。
- 根据真实使用决定小程序或 App。
